{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "ID Card Template 2 Configuration Schema",
  "description": "Schema for configuring ID card template variables - Dark navy with diamond decorations",
  "properties": {
    "colors": {
      "type": "object",
      "properties": {
        "primaryColor": {
          "type": "string",
          "format": "color",
          "title": "Primary Color",
          "description": "Primary color used for header, footer, photo border, and diamond decorations",
          "default": "#1E293B"
        },
        "secondaryColor": {
          "type": "string",
          "format": "color",
          "title": "Secondary Color",
          "description": "Secondary color used for photo placeholder background",
          "default": "#F1F5F9"
        }
      },
      "required": ["primaryColor", "secondaryColor"],
      "additionalProperties": false
    },
    "typography": {
      "type": "object",
      "properties": {
        "textColorForHeaderAndFooter": {
          "type": "string",
          "format": "color",
          "title": "Text Color for Header and Footer",
          "description": "Text color used for header and footer text, and body background",
          "default": "#FFFFFF"
        },
        "defaultTextColor": {
          "type": "string",
          "format": "color",
          "title": "Default Text Color",
          "description": "Default text color used for main content text (name, ID, class, etc.)",
          "default": "#1E293B"
        },
        "defaultLabelColor": {
          "type": "string",
          "format": "color",
          "title": "Default Label Color",
          "description": "Default label color used for field labels",
          "default": "#64748B"
        },
        "bloodGroupTextColor": {
          "type": "string",
          "format": "color",
          "title": "Blood Group Text Color",
          "description": "Text color specifically for blood group value",
          "default": "#EF4444"
        }
      },
      "required": [
        "textColorForHeaderAndFooter",
        "defaultTextColor",
        "defaultLabelColor",
        "bloodGroupTextColor"
      ],
      "additionalProperties": false
    },
    "text": {
      "type": "object",
      "properties": {
        "footerText": {
          "type": "string",
          "title": "Footer Text",
          "description": "Footer text to be displayed at the bottom of the card",
          "default": "Valid Until: 2030"
        }
      },
      "required": ["footerText"],
      "additionalProperties": false
    },
    "schoolLogoFileId": {
      "type": "string",
      "title": "School Logo for ID Card",
      "description": "File ID for the school logo image displayed in the header"
    }
  },
  "required": ["colors", "typography", "text"],
  "additionalProperties": false
}
