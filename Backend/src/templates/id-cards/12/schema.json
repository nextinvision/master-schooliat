{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "ID Card Template 12 Configuration Schema",
  "description": "Schema for configuring ID card template variables - Maple Valley blue/orange style",
  "properties": {
    "colors": {
      "type": "object",
      "properties": {
        "primaryColor": {
          "type": "string",
          "format": "color",
          "title": "Primary Color",
          "description": "Primary color used for header gradient and accents",
          "default": "#2563EB"
        },
        "secondaryColor": {
          "type": "string",
          "format": "color",
          "title": "Secondary Color",
          "description": "Secondary color used for photo placeholder background",
          "default": "#DBEAFE"
        },
        "accentColor": {
          "type": "string",
          "format": "color",
          "title": "Accent Color",
          "description": "Accent color used for blood group badge and emergency card accent",
          "default": "#F97316"
        }
      },
      "required": ["primaryColor", "secondaryColor", "accentColor"],
      "additionalProperties": false
    },
    "typography": {
      "type": "object",
      "properties": {
        "textColorForHeaderAndFooter": {
          "type": "string",
          "format": "color",
          "title": "Text Color for Header and Footer",
          "description": "Text color used for header and footer text",
          "default": "#FFFFFF"
        },
        "defaultTextColor": {
          "type": "string",
          "format": "color",
          "title": "Default Text Color",
          "description": "Default text color used for main content values",
          "default": "#1F2937"
        },
        "defaultLabelColor": {
          "type": "string",
          "format": "color",
          "title": "Default Label Color",
          "description": "Default label color used for field labels",
          "default": "#6B7280"
        },
        "bloodGroupTextColor": {
          "type": "string",
          "format": "color",
          "title": "Blood Group Text Color",
          "description": "Text color for blood group badge",
          "default": "#FFFFFF"
        }
      },
      "required": [
        "textColorForHeaderAndFooter",
        "defaultTextColor",
        "defaultLabelColor",
        "bloodGroupTextColor"
      ],
      "additionalProperties": false
    },
    "text": {
      "type": "object",
      "properties": {
        "footerText": {
          "type": "string",
          "title": "Footer Text",
          "description": "Footer text to be displayed at the bottom of the card",
          "default": "Valid Until: 2030"
        }
      },
      "required": ["footerText"],
      "additionalProperties": false
    },
    "schoolLogoFileId": {
      "type": "string",
      "title": "School Logo for ID Card",
      "description": "File ID for the school logo image displayed in the header"
    }
  },
  "required": ["colors", "typography", "text"],
  "additionalProperties": false
}
